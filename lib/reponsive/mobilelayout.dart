import 'package:flutter/material.dart';

class mobilelayout extends StatelessWidget {
  const mobilelayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('this is mobiole'),
    );
  }
}
